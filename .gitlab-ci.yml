stages:
  - build
  - deploy

build:
  stage: build
  image: cr.yandex/crprd8fjvm5h9q7qgndk/node-with-tools
  script:
      - npm install
      - npm run build
      - ls build
      - envsubst </root/.s3cfg >/root/.s3cfg.tmp && mv -f /root/.s3cfg.tmp /root/.s3cfg
